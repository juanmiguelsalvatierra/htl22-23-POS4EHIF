
public abstract interface Mutateable {   // automatisch abstract, Sichtbarkeit: public oder package locale
	int LEVELFAKTOR=15;  // Konstante, muss im Interface initiiert werden, automatisch public static final
	void levelUp();      // automatisch abstract, Sichtbarkeit:public oder package locale 
}
